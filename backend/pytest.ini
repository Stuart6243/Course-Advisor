[pytest]
# 没有这段配置时，pytest-asyncio 未安装/未启用会让 @pytest.mark.asyncio 的测试
# 以 "async def functions are not natively supported" 静默失败，
# 表面上"测试通过"，实际上所有异步测试根本没跑。
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
